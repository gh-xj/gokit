# file-sync example

Use-case: deterministic local file synchronization commands.

Typical commands:
- `scan`
- `sync`
- `verify`

Contract notes:
- `--json` output for machine consumers
- stable exit code mapping for CI automation
