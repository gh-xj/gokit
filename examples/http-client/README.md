# http-client example

Use-case: CLI wrapper around HTTP APIs with schema-backed output.

Typical commands:
- `request`
- `health`
- `version`

Contract notes:
- deterministic JSON envelopes
- fixed error format for agent parsing
